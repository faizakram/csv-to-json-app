✅ Correct Pure-FTPd Command (no username, no password)

Please stop old containers:

docker stop ftp-anon-only
docker rm ftp-anon-only


Now run this instead:

docker run -d `
  --name ftp-anon-only `
  -p 21:21 -p 20:20 -p 21100-21110:21100-21110 `
  -v C:\ftp\upload:/var/ftp:rw `
  stilliard/pure-ftpd:hardened `
  pure-ftpd -E -A -x -j -R -P 192.168.2.7 -p 21100:21110

What these flags mean:

-E → only anonymous logins allowed

-A → don’t ask for password

-x → disable downloads (upload-only)

-j → chroot (stay inside folder)

-R → disallow symlinks

-P → public IP (192.168.2.7)

-p → passive port range

🔑 Now try in Explorer

Go to:

ftp://192.168.2.7/


👉 It should not ask for username or password, just open directly.
Everything uploaded will go into:

C:\ftp\upload


⚡ If you still see the login popup:

Check logs:

docker logs ftp-anon-only


Sometimes Windows Explorer caches old auth → try Win + R → type:

explorer ftp://192.168.2.7/


(instead of the normal Explorer window).


docker run -d `
  --name pure-ftp `
  -e FTP_USER_NAME=myuser `
  -e FTP_USER_PASS=mypassword `
  -e FTP_USER_HOME=/home/ftpusers/myuser `
  -p 21:21 -p 30000-30009:30000-30009 `
  -v C:\ftp\data:/home/ftpusers/myuser `
  stilliard/pure-ftpd:hardened


docker stop pure-ftp
docker rm pure-ftp

docker run -d `
  --name pure-ftp `
  -e FTP_USER_NAME=root `
  -e FTP_USER_PASS=root `
  -e FTP_USER_HOME=/home/ftpusers/myuser `
  -p 21:21 -p 30000-30009:30000-30009 `
  -v C:\ftp\data:/home/ftpusers/myuser `
  stilliard/pure-ftpd:hardened `
  pure-ftpd -E -j -R -P 192.168.2.7 -p 30000:30009


✅ Correct Way to Set Up Pure-FTPd with User/Password
1. Run the container
docker run -d `
  --name pure-ftp `
  -p 21:21 -p 30000-30009:30000-30009 `
  -v C:\ftp\data:/home/ftpusers/myuser `
  -e PUBLICHOST=192.168.2.7 `
  stilliard/pure-ftpd:hardened

2. Create FTP User inside container

Now exec into the container and add a virtual user:

docker exec -it pure-ftp pure-pw useradd myuser -u ftpuser -d /home/ftpusers/myuser


It will ask for a password (set mypassword).

Then build the Pure-FTPd database:

docker exec -it pure-ftp pure-pw mkdb


Restart the container:

docker restart pure-ftp

3. Connect

Now in FileZilla / Explorer:

Host: 192.168.2.7

Username: myuser

Password: mypassword

Port: 21

⚡ Why your command failed

The -e FTP_USER_NAME and -e FTP_USER_PASS method doesn’t work with this hardened Pure-FTPd image.

You must use the pure-pw tool inside the container to create accounts.


